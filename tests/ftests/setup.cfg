# This file contains test case discovery rules for py.test module
# can also be defined in in tox.ini or pytest.ini file
[pytest]
python_files=xi_ftest_*.py
# python_functions=xi_ftest_*     # Unfortunately this has no effect on the functions of unittest.TestCase
                                  # ... and if the test files already start with "xi_ftest* there is no
                                  # point in requiring the test functions to start so. But anyway, here
                                  # is the option.
